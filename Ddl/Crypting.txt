CREATE TABLE VazcoTable
   (id int IDENTITY PRIMARY KEY NOT NULL,
    data varchar(MAX),
	name varchar(MAX),
	addingTime datetime,
    hash varchar(MAX)
);


----- postgresql

drop table VazcoTable;

CREATE TABLE VazcoTable
   (id serial PRIMARY KEY NOT NULL,
	data text,
	name text,
	addingTime timestamp,
	hash text
);